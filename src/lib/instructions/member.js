define([
        'vendor/underscore',
    ], function(_) {
        return {
            IncDecM: function(op, locDescMVector){
                //TODO: start with implementing base functions
                /*
                switch(op){
                    case "PreInc": 
                        
                        break;
                    case "PostInc":
                        
                        break;
                    case "PreDec":
                        
                        break;
                    case "PostDec":
                        
                        break;
                    default:
                        this.warning("No such operation in IncDecM: " + op);
                }
                */
            }
            //TODO: implement missing functions
        };
    }
);
